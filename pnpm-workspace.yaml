packages:
  - "packages/*"
# esbuild's postinstall fetches its platform binary; required by vitest.
# The only approved build script in this workspace.
allowBuilds:
  argon2: true
  better-sqlite3: true
  esbuild: true
