{
  "name": "@chubz/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Pure contract library for the CHUBZ AI Command Center: task states, legal transitions, command grammar, worker manifests. No I/O.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
